import React from "react";
export const CurrentMappingsContext = React.createContext({});
export const PrefixKeysContext = React.createContext([]);
export const LeaderKeysContext = React.createContext([]);
